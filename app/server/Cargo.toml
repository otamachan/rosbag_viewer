[package]
name = "rosbag-viewer-app"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "rosbag-viewer-app"
path = "src/main.rs"

[dependencies]
rosbag-viewer = { path = "../../lib/server" }
rust-embed = "8"

[lints]
workspace = true
